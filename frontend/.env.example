# Copie para .env antes de rodar:
#   cp .env.example .env
#
# Backend em container Docker na mesma rede (caso padrão):
API_BASE=http://firmato-api:8000
#
# Backend rodando fora do Docker no Windows:
# API_BASE=http://host.docker.internal:8000
#
# Backend em outro servidor:
# API_BASE=https://api.seudominio.com
